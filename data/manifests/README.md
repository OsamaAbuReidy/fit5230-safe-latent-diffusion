# Data manifests

Keep prompts and dataset metadata under version control. Keep downloaded datasets, model weights, and generated images out of Git.

The evaluation manifest will be created after the exact licensing, access conditions, and permitted coursework use of the selected benchmark have been checked. It will record prompt identifier, source, category, split, fixed seed, and any exclusions.
