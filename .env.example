# Optional: only needed if the selected model repository requires authenticated access.
HF_TOKEN=

# Optional: required only to reproduce the benchmark's canonical DACA/PGJ
# prompt transformations. Never commit a real key.
OPENAI_API_KEY=

# Alternative attack-generator providers for DACA/PGJ ablations.
DEEPSEEK_API_KEY=
GEMINI_API_KEY=
